# Local storage

The app stores shortened links in `links.json` in this folder.

- `links.json` is created automatically when you save the first link.
- `links.json` is listed in `.gitignore` and is not committed to git.
